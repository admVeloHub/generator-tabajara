# MongoDB Atlas — obrigatório (Vercel + dev local)
MONGO_URI=

# Nomes dos bancos (padrão do Desk)
MONGODB_DB_NAME=b2c_chamados
MONGODB_CADASTROS_DB_NAME=b2c_cadastros

# Gemini — apenas server-side (Vite dev middleware / Vercel serverless)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash
