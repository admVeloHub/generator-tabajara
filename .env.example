# Velodesk API — dev local (browser → host:8001)
VITE_VELODESK_API_URL=http://localhost:8001

# Vercel (server-side proxy /api/velodesk → backend público)
# Obrigatório em produção — NÃO use localhost
VELODESK_API_URL=https://velohub-278491073220.us-east1.run.app

# Gemini — apenas server-side (Vite dev middleware / Vercel serverless)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash
